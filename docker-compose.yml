version: "3.9"

services:
  backend:
    build: ./backend
    container_name: foxtrails-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./web/foxtrails_front
    container_name: foxtrails-frontend
    ports:
      - "80:80"
    depends_on:
      - backend